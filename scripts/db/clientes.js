/*
const insertCliente = app.post("/controllerForm", urlEncodeParser, (req, res) => {
    sql.query("insert into client ('name', 'cpf', 'address', 'phone_number') values (?,?,?,?)", 
    [req.body.nome, req.body.cpf, req.body.endereco, req.body.telefone])
    res.redirect("clientes")
    // adicionar tratamento
})

exports(insertCliente)

*/